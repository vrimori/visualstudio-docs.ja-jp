---
title: オプション ダイアログ ボックスの 全般、デバッグ、|Microsoft ドキュメント
ms.custom: ''
ms.date: 05/23/2017
ms.technology: vs-ide-debug
ms.topic: reference
f1_keywords:
- vs.debug.options.General
- VS.ToolsOptionsPages.Debugger.General
- VS.ToolsOptionsPages.Debugger.ENC
- vs.debug.options.ENC
dev_langs:
- CSharp
- VB
- FSharp
- C++
- JScript
helpviewer_keywords:
- Options dialog box, debugging
ms.assetid: b33aee0b-43c3-4c26-8ed4-bc673f491503
author: mikejo5000
ms.author: mikejo
manager: douge
ms.workload:
- multiple
ms.openlocfilehash: 5b7af8c68764b3a9ed85bf6a52a3a6c4a0568203
ms.sourcegitcommit: 0aafcfa08ef74f162af2e5079be77061d7885cac
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 06/01/2018
ms.locfileid: "34572051"
---
# <a name="general-debugging-options-dialog-box"></a>[全般] ([オプション] ダイアログ ボックス - [デバッグ])
**ツール > オプション > デバッグ > 全般**ページのこの記事で説明するオプションを設定できます。

既定の設定を復元する必要がある場合を使用して行うことができます**ツール** > **インポートおよびエクスポート設定** > **すべての設定をリセット**です。 設定のサブセットをリセットする場合で設定を保存、**インポートおよびエクスポートの設定ウィザード**をテストする、変更を行う前に、保存した設定後でインポートします。
  
**すべてのブレークポイントを削除する前に確認する**で完了する前に確認が必要、**すべてのブレークポイントの削除**コマンド。  
  
**1 つのプロセスがブレークするときに、すべてのプロセスをブレーク**同時に、デバッガーをアタッチする、中断が生じたときにすべてのプロセスを中断します。  
  
**例外が AppDomain またはマネージ/ネイティブの境界を越えるときに中断**マネージまたは混合モード デバッグで、共通言語ランタイムによりアプリケーション ドメインの境界、またはマネージ/ネイティブの境界を越える例外がキャッチときに、次条件に該当します。  
  
1\)ネイティブ コードでは、マネージ コードを呼び出す COM 相互運用機能を使用して、マネージ コードが例外をスローした場合します。 参照してください[COM 相互運用機能の概要](/dotnet/articles/visual-basic/programming-guide/com-interop/introduction-to-com-interop)です。  
  
2\)アプリケーション ドメイン 1 で実行されるマネージ コードがアプリケーション ドメイン 2、マネージ コードを呼び出し、アプリケーション ドメイン 2 のコードが例外をスローします。 参照してください[アプリケーション ドメインを使用したプログラミング](/dotnet/articles/framework/app-domains/index)です。  

3\)コードがリフレクションを使用して関数を呼び出し、関数が例外をスローします。 参照してください[リフレクション](/dotnet/framework/reflection-and-codedom/reflection)です。  
  
2 および 3 の状態で例外が場合がありますのマネージ コードによりキャッチ`mscorlib`共通言語ランタイムの代わりにします。 このオプションを選択しても、`mscorlib` でキャッチされる例外の処理は中断されません。  
  
**アドレス レベルのデバッグを有効にする**、アドレス レベル デバッグ用の機能を強化できるように (、**逆アセンブル** ウィンドウで、**登録**ウィンドウ、およびアドレス ブレークポイント)。  
  
- **ソースがないかどうかは逆アセンブルの表示**が自動的に表示、**逆アセンブル**ウィンドウのソース コードをデバッグするときは使用できません。  
  
**ブレークポイントのフィルターを有効にする**特定のプロセス、スレッド、またはコンピューターのみが影響するために、ブレークポイントのフィルターを設定することができます。  
 
**新しい例外ヘルパーを使用して**例外処理アシスタントを置換すると例外ヘルパー (Visual Studio 2017) を有効にします。
  
> [!NOTE]
> マネージ コードの場合は、このオプションが既に呼び出されて**例外処理アシスタントを有効にする**です。 
  
**マイ コードのみを有効にする**表示し、システム コードや、最適化されたデバッグ シンボルがないその他のコードを無視してユーザー コード (「マイ コード」) のみに手順を説明デバッガーです。

- **ユーザー コードが見つからない (マネージのみ) 起動時に警告する**場合デバッグの開始 マイ コードのみが有効になっていると、このオプションを警告するかどうかは、ユーザー コード (「マイ コード」) はありません。 

**.NET Framework を有効にするソースのステッピング**により、デバッガーは、.NET Framework のソースにステップ インします。 このオプションを有効にすると、[マイ コードのみ] が自動的に無効になります.NET Framework シンボルは、キャッシュの場所へダウンロードされます。 キャッシュの場所を変更することができます、**オプション**ダイアログ ボックスで、**デバッグ**カテゴリ、**シンボル**ページ。  
  
**プロパティおよび演算子 (マネージのみ) をステップ オーバー**デバッガーがプロパティとマネージ コード内の演算子にステップ インすることを防止します。  
  
**プロパティの評価とその他の暗黙的な関数呼び出しを有効にする**変数ウィンドウに呼び出しを有効に自動プロパティの評価と暗黙的な関数と**クイック ウォッチ**  ダイアログ ボックス。  
  
- **変数ウィンドウ (c# および JavaScript のみ) でオブジェクトに対して文字列変換関数を呼び出す**変数ウィンドウ内のオブジェクトを評価するときに、暗黙的な文字列変換呼び出しを実行します。 結果は、型名ではなく文字列として表示されます。 C# コードのデバッグ時にのみ適用されます。 この設定は、DebuggerDisplay 属性によってオーバーライドされる可能性があります (を参照してください[DebuggerDisplay 属性を使用して](../debugger/using-the-debuggerdisplay-attribute.md))。  
  
**ソース サーバー サポートを有効にする**SrcSrv を実装するソース サーバーからソース ファイルを取得する Visual Studio デバッガーに指示 (`srcsrv.dll`) プロトコルです。 このプロトコルを実装する 2 つのソース サーバーは Team Foundation Server と Debugging Tools for Windows です。 SrcSrv のセットアップの詳細については、次を参照してください。、 [SrcSrv](https://msdn.microsoft.com/library/windows/hardware/ff558791(v=vs.85).aspx)ドキュメント。 さらを参照してください[指定シンボル (.pdb) とソース ファイル](../debugger/specify-symbol-dot-pdb-and-source-files-in-the-visual-studio-debugger.md)です。  
  
> [!IMPORTANT]
>  .pdb ファイルを読み取ることでそのファイル内の任意のコードを実行できるため、サーバーが信頼されていることを確認してください。  
  
- **ソース サーバー診断メッセージを出力ウィンドウを**診断表示をソース サーバー サポートを有効にすると、この設定がオンにします。  
  
- **部分信頼アセンブリ (マネージのみ) のソース サーバーを許可する**ソース サーバー サポートを有効にすると、この設定は、部分信頼アセンブリのソースを取得しないの既定の動作をオーバーライドします。  

**ソースのリンクのサポートを有効にする**ソース リンク 情報が含まれる .pdb ファイルのソース ファイルをダウンロードする Visual Studio デバッガーに指示します。 ソースのリンクの詳細については、次を参照してください。、[送信元リンク仕様](https://github.com/dotnet/core/blob/master/Documentation/diagnostics/source_link.md)です。

    > [!IMPORTANT]
    >  Because Source Link will download files using http or https, make sure you trust the .pdb file.  
  
**ブレークポイントおよび現在のステートメント (C++ のみ) のソース行全体を強調表示**デバッガーには、ブレークポイントまたは現在のステートメントが強調表示、ときに行全体を強調表示します。  
  
**元のバージョンと完全に一致するソース ファイルを必要と**ソース ファイルが、ソース コードをデバッグする実行可能ファイルをビルドするために使用のバージョンと一致していることを確認するデバッガーに指示します。 バージョンが一致しない場合、一致するソースを見つけるに求められます。 一致するソースが見つからないときは、デバッグ時にソース コードが表示されません。 
  
**イミディ エイト ウィンドウにすべての出力ウィンドウのテキストをリダイレクト**デバッガーに通常表示されるメッセージをすべての送信、**出力**ウィンドウ、**イミディ エイト**ウィンドウ代わりにします。  
  
**オブジェクトの生の構造体を変数ウィンドウに表示する**オブジェクト構造ビューのすべてのカスタマイズをオフにします。 ビューのカスタマイズの詳細については、次を参照してください。 [.managed オブジェクトのカスタム ビューを作成](../debugger/create-custom-views-of-dot-managed-objects.md)です。  
  
**モジュールを読み込めません (マネージのみ) に JIT 最適化を抑制する**モジュールが読み込まれ、デバッガーがアタッチされている間、JIT のコンパイルするときに、マネージ コードの JIT 最適化を無効にします。 最適化を無効にした場合、一部の問題のデバッグは簡単になりますが、パフォーマンスは低下します。 [マイ コードのみ] を使用しているときに JIT 最適化を抑制すると、非ユーザー コードがユーザー コード ("マイ コード") として表示される可能性があります。 詳細については、次を参照してください。 [JIT の最適化とデバッグ](../debugger/jit-optimization-and-debugging.md)です。

**JavaScript のデバッグ (Chrome および IE) の ASP.NET を有効にする**ASP.NET アプリ用のスクリプト デバッガーを有効にします。 Chrome で最初に使用するときに、インストールされている Chrome 拡張機能を有効にする最初の使用のブラウザーにサインインする必要があります。 従来の動作に戻すには、このオプションを無効にします。    

**Dll エクスポートを読み込む**dll エクスポート テーブルを読み込みます。 DLL エクスポート テーブルのシンボル情報は、対応するシンボルのない Windows メッセージ、Windows プロシージャ (WindowProc)、COM オブジェクト、マーシャリング、DLL を操作する場合に役立ちます。 DLL エクスポート情報を読み取ると、オーバーヘッドがある程度発生します。 そのため、既定ではこの機能はオフになっています。  
  
Dll のエクスポート テーブルで使用できるシンボルを確認するには、次のように使用します。`dumpbin /exports`です。 シンボルは、すべての 32 ビット システムの DLL に使うことができます。 `dumpbin /exports` の出力を参照すると、英数字以外の文字を含む、正確な関数名を確認できます。 この情報は、関数にブレークポイントを設定するときに使用します。 DLL エクスポート テーブルの関数名は、デバッガーの他の場所で表示されるとき、切り捨てられることがあります。 関数は呼び出し順に表示され、現在の関数 (入れ子の一番内側) が先頭に表示されます。 詳細については、「 [dumpbin /exports](/cpp/build/reference/dash-exports)」を参照してください。  
  
**表示する並列スタックの図を上下**でスタックが表示される方向を制御、**並列スタック**ウィンドウです。
  
**書き込まれるデータ値が変更されなかった場合は、GPU メモリ アクセス例外を無視して**データが変更されなかった場合、デバッグ時に検出された競合状態は無視されます。 詳細については、次を参照してください。 [GPU コードのデバッグ](../debugger/debugging-gpu-code.md)です。  
  
**マネージ互換モードを使用して**デバッグ エンジンをレガシ バージョンをこれらのシナリオを有効にすると既定値が置き換えられます。  
  
- 独自の式エバリュエーターを提供する C#、VB、または F# 以外の .NET Framework 言語を使用しています (これは C++/CLI を含みます)。  
  
- 混合モード デバッグ時に、C++ プロジェクトのエディット コンティニュを有効にします。  
  
> [!NOTE]
> マネージ互換を選択するモードには、既定のデバッグ エンジンにのみ実装されている一部の機能が無効になります。 

**従来の c# および VB の式エバリュエーターを使用して**デバッガーは Visual Studio 2015 の Roslyn ベースの式エバリュエーターではなく Visual Studio 2013 の C #/vb の式エバリュエーターを使用します。    
  
**危険性プロセス (マネージのみ) に対してカスタムのデバッガー ビジュアライザーを使用する場合に警告**unsafe に実行される可能性があるために、コードをデバッグ対象のプロセスで実行されているカスタムのデバッガー ビジュアライザーを使用しているときに Visual Studio 警告します。コードです。  
  
**Windows デバッグ ヒープ アロケーター (ネイティブのみ) を有効にする**ヒープ診断を向上させるために windows デバッグ ヒープを有効にします。 このオプションを有効にすると、デバッグ パフォーマンスに影響を与えます。  
  
**XAML の UI デバッグ ツールを有効にする**デバッグを開始する (F5)、サポートされているプロジェクトの種類と、ライブ ビジュアルのツリーとライブ プロパティ エクスプ ローラー ウィンドウが表示されます。 詳細については、次を参照してください。[検査 XAML のプロパティはデバッグ中に](../debugger/inspect-xaml-properties-while-debugging.md)です。  
  
- **Live Visual Tree で選択した要素をプレビュー**コンテキストが選択されている XAML 要素は選択されても、 **Live Visual Tree**ウィンドウです。  
  
- **アプリケーションでランタイム ツールを表示する**を示しています、 **Live Visual Tree**ツールバーは、デバッグ中の XAML アプリケーションのメイン ウィンドウでコマンド。 このオプションは、Visual Studio 2015 Update 2 で導入されました。 

- **XAML エディット コンティニュを有効にする**編集を使用して XAML コードの機能を続行することができます。 
  
**デバッグ中に診断ツールを有効にする**、**診断ツール**は、デバッグ中にウィンドウが表示されます。
  
**デバッグ中に経過時間の PerfTip を表示**をデバッグする場合、コード ウィンドウが特定のメソッドの呼び出しの経過時間を表示します。  
  
**エディット コンティニュを有効にする**編集に使用でき、デバッグ中に機能を続行します。  
  
- **ネイティブのエディット コンティニュを有効にする**編集を使用してネイティブ C++ コードのデバッグ中に機能を続行します。 詳細については、次を参照してください。[エディット コンティニュ (Visual c)](../debugger/edit-and-continue-visual-cpp.md)です。  
  
- **変更を適用 (ネイティブのみ) をコンティニュに**Visual Studio は自動的にコンパイルし、ブレーク状態からプロセスを続行する場合に対して行ったすべての未処理のコード変更を適用します。 選択されていない場合は デバッグ メニューで コードの変更を適用する"アイテムを使用して変更を適用することもできます。  
  
- **古いコード (ネイティブのみ) に関する警告**古いコードに関する警告を取得します。    

**表示ボタンをクリックしてエディターでのデバッグ中に実行**は、このオプションを選択すると、[実行 をクリックする](debugger-feature-tour.md#run-to-a-point-in-your-code-quickly-using-the-mouse)デバッグ中にボタンが表示されます。

## <a name="options-supported-in-older-versions-of-visual-studio"></a>Visual Studio の旧バージョンでサポートされているオプション

古いバージョンの Visual Studio を使用している場合、追加のオプションが存在する可能性があります。

**例外処理アシスタントを有効にする**、マネージ コードの例外処理アシスタントを有効にします。 Visual Studio 2017 では、例外ヘルパーには、例外処理アシスタントが置き換えられます。

**ハンドルされない例外で呼び出し履歴をアンワインド**により、**呼び出し履歴**にロールバックする呼び出しスタック、ポイント、ハンドルされない例外が発生する前にウィンドウです。 

**見つからないとき警告 (ネイティブのみ) の起動時にシンボルが**をデバッガー シンボル情報いないプログラムをデバッグしようとすると、警告ダイアログ ボックスが表示されます。 

**起動時にスクリプトのデバッグが無効になっているかどうかに警告**スクリプトのデバッグを無効になっていると、デバッガーが起動されたときに、警告ダイアログ ボックスが表示されます。

**ネイティブ互換モードを使用して**デバッガーで Visual Studio 2010 のネイティブ デバッガーを使用して、新しいネイティブ デバッガーの代わりにこのオプションを選択するとします。  
  
新しいデバッグ エンジンは .NET C++ 式の評価をサポートしていないため、.NET C++ コードをデバッグするときにはこのオプションを使用する必要があります。 ただし、ネイティブ互換モードを有効にすると、現在のデバッガーの実装に依存している多くの機能が無効になります。 たとえば、レガシ エンジンではなどの組み込み型の場合、多くのビジュアライザーがない`std::string`の Visual Studio 2015 のプロジェクトです。   このような場合に最適なデバッグ機能を Visual Studio 2013 プロジェクトを使用します。
  
## <a name="see-also"></a>関連項目  
 [Visual Studio でのデバッグ](../debugger/index.md)  
 [デバッガー機能ツアー](../debugger/debugger-feature-tour.md)
